var nombreUsuario = "kevin15.";

function autenticacion(usuario) {
    usuario;
    if (nombreUsuario===usuario) {
        alert ("autenticacion correcta");
        }else (
            alert("autenticaicon incorrecta");
        )
}
autenticacion(prompt("ingrese usuario"));